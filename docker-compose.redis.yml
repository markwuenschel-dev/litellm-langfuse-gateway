# Thin DX shim — canonical Redis overlay lives in infra/llm-gateway/
#
#   docker compose -f docker-compose.yml -f docker-compose.redis.yml up -d

include:
  - path: infra/llm-gateway/compose.redis.yaml
