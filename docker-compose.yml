# Thin DX shim — canonical stack lives in infra/llm-gateway/
#
# Usage (from repo root):
#   cp infra/llm-gateway/.env.example .env   # or use root .env.example
#   docker compose up -d
#
# With Redis:
#   docker compose -f docker-compose.yml -f docker-compose.redis.yml up -d
#
# Prefer editing files under infra/llm-gateway/ only.

include:
  - path: infra/llm-gateway/compose.yaml
