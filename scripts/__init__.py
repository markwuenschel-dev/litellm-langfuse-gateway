"""Ops helpers for the LiteLLM + Langfuse gateway.

Prefer the consolidated CLI:

    uv run llg --help
    uv run llg config validate
    uv run llg secrets generate
    uv run llg health
    uv run llg up | down

These modules remain as thin re-exports for backward compatibility.
"""
